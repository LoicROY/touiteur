extends ../layout

block content

  div.touite-form
    form(method="post" action=`/touites/edit/${touite._id}`)
      h1 Editer un touite
      div
        textarea(
          name="content"
          placeholder="Quoi de neuf ?")= touite.content
      
      
      div: button(type="submit") Valider les modifications